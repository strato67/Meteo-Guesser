version: "3.8"

services:
  city-gen-service:
    build:
      context: ./city-gen-service
      target: development
    volumes:
      - ./city-gen-service:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 3000:3000
    command: npm run dev  